name: runner-smoke

on: workflow_dispatch

jobs:
  smoke:
    runs-on: homelab-k3s
    steps:
      - uses: actions/checkout@v5
      - name: prove runner + dind work
        run: |
          echo "runner: $(hostname)"
          uname -a
          docker info --format 'docker server: {{.ServerVersion}}'
